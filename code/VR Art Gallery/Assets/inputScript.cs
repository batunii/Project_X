using UnityEngine;

public class inputScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Input Manager has started!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
